# Plain-pip fallback. The canonical setup is uv (`uv sync`, see README); pyproject.toml holds the
# authoritative dependency list and uv.lock the exact pinned versions. This file mirrors the
# pyproject core deps for `pip install -r requirements.txt` when uv is not available.
# The geospatial wheels bundle their own GDAL/GEOS/PROJ — no system GDAL needed.
torch>=2.2
torchvision>=0.17
pytorch-lightning>=2.0
torchmetrics>=1.0
gpytorch>=1.11
linear-operator>=0.5
geopandas>=0.14
pyogrio>=0.7
rasterio>=1.3
shapely>=2.0
pyproj>=3.5
scikit-image>=0.21
scikit-learn>=1.1
scipy>=1.10
pandas>=1.5
numpy>=1.24,<2.0
h5py>=3.8
matplotlib>=3.7
pyyaml>=6.0
tqdm>=4.65
psutil>=5.9
datasets>=2.10
wandb>=0.15        # optional: experiment tracking (enable with USE_WANDB=true)
