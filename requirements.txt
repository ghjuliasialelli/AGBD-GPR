# pip fallback (conda via environment.yml is recommended for GDAL/rasterio/pytorch).
# Tested-compatible minimums; for exact reproduction use a lockfile (pip freeze > requirements.lock.txt).
torch>=2.0
torchvision>=0.15
pytorch-lightning>=2.0
torchmetrics>=1.0
gpytorch>=1.11
geopandas>=0.13
rasterio>=1.3
scikit-image>=0.21
scikit-learn>=1.1
scipy>=1.10
pandas>=1.5
numpy>=1.24,<2.0
h5py>=3.8
matplotlib>=3.7
seaborn>=0.12
shapely>=2.0
pyproj>=3.5
tqdm>=4.65
psutil
wandb>=0.15        # optional: experiment tracking (enable with USE_WANDB=true)
